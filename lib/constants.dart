import 'package:flutter/material.dart';

const kWhiteColor = Colors.white;
const kLightBlueAccentColor = Colors.lightBlueAccent;
const kBlackColor = Colors.black;

const kTodoeyTextStyle = TextStyle(color: kWhiteColor,fontSize: 30,fontWeight: FontWeight.w700);
const kTasksTextStyle = TextStyle(color: kWhiteColor,fontSize: 18);
const kContainerBoxDecoration =  BoxDecoration(color: kWhiteColor,borderRadius: BorderRadius.only(topLeft: Radius.circular(20),topRight: Radius.circular(20),));